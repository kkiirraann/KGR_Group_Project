from django.urls import path
from user import views as user_views


urlpatterns = [
  # path('login/', user_views.index, name='user-login'),
  # path('login1/', user_views.login, name='login'),
  # path('register/', user_views.register, name="register"),
  #path('index/',user_views.index, name="user-index")

]